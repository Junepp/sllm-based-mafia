_type: "prompt"

template: "
  다음 규칙에 따라 발언자를 선택할 것.
  - 후보 정보(성격 및 성향)과 메세지 히스토리를 참고하여 선택할것.  
  - 발언자는 후보 중에서 한 명만 선택할것.

  후보 정보: {player_info}

  메세지 히스토리: {message_history}

  {format_instructions}

  다음 발언자:
  "

input_variables: ["player_info", "message_history"]
